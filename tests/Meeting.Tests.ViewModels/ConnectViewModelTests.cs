using Meeting.Core.Common;
using Meeting.Wpf.ViewModels;
using Moq;
using Xunit;

namespace Meeting.Tests.ViewModels
{
    public class ConnectViewModelTests
    {
        //[Fact]
        //public void JoinCommand()
        //{
        //    //Arrange
        //    var mockWindowService = new Mock<IMeetingAuthorization>();
        //    mockWindowService.Setup(w => w.ShowWindow<AboutWindow>()).Verifiable();
        //    var mockApplicationService = new Mock<IApplicationService>();

        //    //Act
        //    var mainViewModel = new ConnectViewModel();
        //    mainViewModel.ShowAboutCommand.Execute(null);

        //    //Assert
        //    mockWindowService.Verify(w => w.ShowWindow<AboutWindow>(), Times.AtLeastOnce);
        //}
    }
}
